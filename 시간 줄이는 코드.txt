시간 줄이는 함수

----------------------------------------------------------------------------
1. 
a ** b == pow(a, b)

** 사용하는 것보다  pow 함수를 사용하면 더 빠르다.
※ 추가적으로 % 나누기(만) 사용 가능하다.
pow(a, b, 10)

----------------------------------------------------------------------------
2. 
import sys
int = sys.stdin.readline
data = int(input())

input()함수보다 sys.stdin.readline을 사용하면 더 빠르다.
