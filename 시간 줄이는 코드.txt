시간 줄이는 함수

----------------------------------------------------------------------------
1. 
a ** b == pow(a, b)

** 사용하는 것보다  pow 함수를 사용하면 더 빠르다.
※ 추가적으로 % 나누기(만) 사용 가능하다.
pow(a, b, 10)

----------------------------------------------------------------------------
2. 
import sys
int = sys.stdin.readline
data = int(input())

input()함수보다 sys.stdin.readline을 사용하면 더 빠르다.

----------------------------------------------------------------------------
3. 
a = b = 0

----------------------------------------------------------------------------
4. 
문자열이 정수 또는 실수인지 확인시 .isdecimal()을 사용한다.
a = 12.124
print(a.isdecimal())

>>
False

----------------------------------------------------------------------------
5.
입력할 문자열 하나하나 리스트와 하기
s = list(map(str, input()))
→안녕하세요 입력

>>
['안', '녕', '하', '세', '요']

----------------------------------------------------------------------------
6.
입력 숫자를 자리수마다 더하기
num = sum((map(int, input())))

----------------------------------------------------------------------------
7. 
replace()함수를 이용해 1 → 5로 바꿀 수 있다.(비파괴적 함수)
a = '12345'
print(a.replace('1', '5'))

>>
52345

----------------------------------------------------------------------------
8. 
아스키코드를 문자로 변환할때 chr()코드 대신 print('%c' %d)로 표현가능
print('%c' %d(46))

>> .

----------------------------------------------------------------------------
9.
2차원 리스트의 인덱스 값 구하기 외우기
result = [[i+1,j+1] for i in range(n) for j in range(n) if arr[i][j]==m]